// Declarar una función
function saludar(nombre) {
    console.log(`¡Hola, ${nombre}!`);
  }
  
  const numeros = [1, 2, 3, 4, 5];

numeros.forEach(function(numero) {
  saludar("pablo" + numero);
});

  


